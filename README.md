# -
This project implements a deep learning–based Convolutional Neural Network (CNN) for the classification of synthetic foliage military targets using radar-based synthetic image data. The model is trained and tested in MATLAB, achieving 97% accuracy for automated target detection and surveillance applications.
